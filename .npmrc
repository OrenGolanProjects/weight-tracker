# Use a flat (hoisted) node_modules layout so transitive @types/* resolve for
# `tsc` (avoids TS2688 "Cannot find type definition file" under pnpm's default
# isolated layout). Matches how this project was previously built with npm.
node-linker=hoisted
